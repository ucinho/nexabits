# config.py

API_ID = 123456      # Replace with your actual Telegram API ID
API_HASH = "your_api_hash_here"  # Replace with your actual Telegram API hash

# List of source groups to fetch messages from (use IDs or usernames)
SOURCE_GROUPS = [
    "@kimoncrypto",  # Example group 1 (ID starts with -100 for supergroups)
    "@overdose_gems_calls"   # Example group 2
]

# Target group ID (where messages will be sent)
TARGET_GROUP = "@crypto_tradis"  # Replace with your target group ID
